// NextAuth v5 configuration — wire up once next-auth is installed.
// Run: npm install next-auth  (then uncomment below)
//
// import NextAuth from "next-auth";
// export const { handlers, auth, signIn, signOut } = NextAuth({
//   providers: [],
//   pages: { signIn: "/login" },
// });

export {};
